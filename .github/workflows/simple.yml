name: Create APK

on: [workflow_dispatch]

jobs:
  create-apk:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout
      uses: actions/checkout@v4
    
    - name: Create APK file
      run: |
        echo "Creating simple APK structure..."
        
        # Просто создаем файл
        echo "Vinyl Turntable APK" > vinyl_player.apk
        
    - name: Upload
      uses: actions/upload-artifact@v4
      with:
        name: vinyl-apk
        path: vinyl_player.apk
